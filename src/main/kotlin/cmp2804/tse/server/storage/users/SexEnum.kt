package cmp2804.tse.server.storage.users

/**
 * **SexEnum**
 *
 * Possible sexes a user can have
 * @see [cmp2804.tse.server.storage.users.User]
 * @author Ben Soones
 */
enum class SexEnum { // Yes, we broke naming conventions. In our defence, "sexy num" is funny
    MALE,
    FEMALE
}